"""Completed database migration placeholder."""

import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("migrate")


def migrate() -> None:
    logger.info("No database migrations to apply")


if __name__ == "__main__":
    migrate()

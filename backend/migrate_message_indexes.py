"""Completed message-index migration placeholder."""

import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("message-index-migration")


def migrate() -> None:
    logger.info("No message index migration to apply")


if __name__ == "__main__":
    migrate()
